import 'package:flutter/cupertino.dart';
import 'package:flutter/material.dart';

class ColorsFile {
  static const colorwhite = Color(0XffFFFFFF);   //WHITE
  static const colorblack = Color(0xff000000);   // BLACK
  static const colorred = Colors.red;    // RED COLOR
  static const colorblue = Color(0xff004BAA);
}